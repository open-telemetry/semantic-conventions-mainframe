{
  "__inputs": [{"name":"DS_PROMETHEUS","label":"Prometheus","type":"datasource","pluginId":"prometheus"}],
  "__requires": [
    {"type":"grafana","id":"grafana","name":"Grafana","version":"11.0.0"},
    {"type":"datasource","id":"prometheus","name":"Prometheus","version":"1.0.0"}
  ],
  "uid": "mainframe-crypto",
  "title": "Mainframe — mainframe adapter crypto/flash/RoCE",
  "description": "Auto-generated from mainframe semantic conventions. Classic-mode per-adapter type utilization metrics.",
  "tags": ["mainframe","semconv","mainframe"],
  "schemaVersion": 39,
  "version": 1,
  "refresh": "30s",
  "time": {"from":"now-1h","to":"now"},
  "panels": [
    {
      "id": 1,
      "title": "Fraction of time the classic-mode cryptographic adapter was busy processing requests",
      "description": "Metric: mainframe.adapter.crypto.utilization. Unit: 1 (ratio 0-1). Classic mode, HMC >= 2.12.0.",
      "type": "gauge",
      "gridPos": {"x":0,"y":0,"w":8,"h":8},
      "datasource": {"type":"prometheus","uid":"${DS_PROMETHEUS}"},
      "targets": [{"expr":"mainframe_adapter_crypto_utilization{job=~\".+\"}","legendFormat":"{{adapter}}"}],
      "fieldConfig": {"defaults":{"unit":"percentunit","min":0,"max":1,"custom":{}}}
    },
    {
      "id": 2,
      "title": "Fraction of time the classic-mode Flash Express adapter was busy processing requests",
      "description": "Metric: mainframe.adapter.flash.utilization. Unit: 1 (ratio 0-1). Classic mode, HMC >= 2.12.0.",
      "type": "gauge",
      "gridPos": {"x":8,"y":0,"w":8,"h":8},
      "datasource": {"type":"prometheus","uid":"${DS_PROMETHEUS}"},
      "targets": [{"expr":"mainframe_adapter_flash_utilization{job=~\".+\"}","legendFormat":"{{adapter}}"}],
      "fieldConfig": {"defaults":{"unit":"percentunit","min":0,"max":1,"custom":{}}}
    },
    {
      "id": 3,
      "title": "Fraction of time the classic-mode RoCE adapter was busy processing requests",
      "description": "Metric: mainframe.adapter.roce.utilization. Unit: 1 (ratio 0-1). Classic mode, HMC >= 2.12.1.",
      "type": "gauge",
      "gridPos": {"x":16,"y":0,"w":8,"h":8},
      "datasource": {"type":"prometheus","uid":"${DS_PROMETHEUS}"},
      "targets": [{"expr":"mainframe_adapter_roce_utilization{job=~\".+\"}","legendFormat":"{{adapter}}"}],
      "fieldConfig": {"defaults":{"unit":"percentunit","min":0,"max":1,"custom":{}}}
    }
  ]
}
